# apps/mobile — Expo environment.
#
# EXPO_PUBLIC_* variables are inlined into the JS bundle at build time.
# Mobile bundles are reversible — NEVER put secrets here.
# All sensitive operations must go through apps/server, gated by a user JWT
# stored in expo-secure-store.

EXPO_PUBLIC_API_URL=http://localhost:4000
